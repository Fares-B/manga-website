{% extends 'base.html.twig' %}

{% block title %}
	{{ title }}
{% endblock %}

{% block body %}

	<h3>Connexion sur le site</h3>
	<div class="form-">
		{{ form_start(form) }}
		{{ form_row(form.username) }}
		{{ form_row(form.password) }}
		<button type="submit" class="btn btn-success">Connexion</button>
		{{ form_end(form) }}
	</div>
{% endblock %}

